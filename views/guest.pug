extends layout
block content
    if(bookdetail.length!==0)
        each book in bookdetail
            label SSID: #{book.SSID}
            img(src='http://localhost:8080/images/bookpicture/' + book.book_image , style='height:150px;width:150px', onclick='buying(' + book.SSID + ')')
            label description
            p= book.book_description
            p= book.tag
    else
        h2 no such books you looking for